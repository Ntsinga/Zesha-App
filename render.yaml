services:
  - type: web
    name: teleba-app
    buildCommand: npx expo export --platform web
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
